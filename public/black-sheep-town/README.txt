BLACK SHEEP TOWN v1.2.0 — MAO release and script site

The bilingual reader uses all 29,753 visible executed lines from Steam Build 13300478. The two whitespace-only runtime slots are retained in the patch topology but are not fabricated as reader lines. Inline Tips expose the finalized Japanese and English reference text on hover, keyboard focus, or click. The default-on Display Tips control can render those tagged words as ordinary text instead.
